import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: 'rgb(97 218 251)'
        },
        secondary: {
            main: 'rgb(34, 34, 34)'
        }
    }
});

export default theme;